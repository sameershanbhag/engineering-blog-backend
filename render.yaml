# Render blueprint — provisions a Postgres DB + the FastAPI web service.
# Create via Render dashboard → New → Blueprint → connect this repo.
# Auto-deploys on every push to main.

databases:
  - name: engineering-blog-db
    plan: free

services:
  - type: web
    name: engineering-blog-backend
    runtime: docker
    plan: free
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: APP_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: engineering-blog-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: INTERNAL_API_SECRET
        sync: false  # must match the frontend's INTERNAL_API_SECRET; set in dashboard
      - key: CORS_ORIGINS
        value: "https://engineering-blog-lilac.vercel.app,http://localhost:3000"
